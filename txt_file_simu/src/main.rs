
use std::prelude;
use std::vec;
struct file{
    rows : Vec<Row>,
    filename: Option<String>
}

struct Row{
    string : String
}

impl File for file{
    fn insert(&self,i:i32,c:char){
        self.rows.
    }
}

fn main() {
    println!("Hello, world!");
}
